import { ColorAdressDirective } from './color-adress.directive';

describe('ColorAdressDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorAdressDirective();
    expect(directive).toBeTruthy();
  });
});
