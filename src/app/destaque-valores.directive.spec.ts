import { DestaqueValoresDirective } from './destaque-valores.directive';

describe('DestaqueValoresDirective', () => {
  it('should create an instance', () => {
    const directive = new DestaqueValoresDirective();
    expect(directive).toBeTruthy();
  });
});
