import { EventColorDirective } from './event-color.directive';

describe('EventColorDirective', () => {
  it('should create an instance', () => {
    const directive = new EventColorDirective();
    expect(directive).toBeTruthy();
  });
});
