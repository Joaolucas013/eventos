import { DestaqueTextosDirective } from './destaque-textos.directive';

describe('DestaqueTextosDirective', () => {
  it('should create an instance', () => {
    const directive = new DestaqueTextosDirective();
    expect(directive).toBeTruthy();
  });
});
